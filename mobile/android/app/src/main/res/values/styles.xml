<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 11 and earlier: the whole launch screen is this drawable. -->
        <item name="android:background">@drawable/splash</item>
        <!-- Android 12 and later: the system draws the launcher icon itself,
             and only wants to be told what to draw it on. Left unset it is
             white, which is a flash of the wrong colour on the way in. -->
        <item name="windowSplashScreenBackground">@color/astra_ground</item>
    </style>
</resources>